# 统一按 LF 入库/检出：避免 Windows 上的 CRLF 转换造成「仓库与线上/台账字节不一致」
# （本项目把源码副本逐字节比对并计入 SHA256 台账，行尾漂移会直接破坏该一致性证据）
* text=auto eol=lf

# 二进制/压缩资源不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.pdf binary
*.mp4 binary
*.zip binary
*.woff2 binary
