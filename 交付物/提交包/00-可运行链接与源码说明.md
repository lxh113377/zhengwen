# 可运行链接与源码说明

> 用途：满足 29 号第 6 页官方要求「若作品为可运行的程序，需提供可运行的链接或应用程序源代码。对于网页应用，确保链接稳定可访问。」
> 建立：2026-09-18 ｜ 状态：**✅ 三层入口全部就位并实测通过**（含公网只读入口与离线静态快照）

---

## 一、官方要求原文（29 号第 6 页）

> 「（三）可运行程序：若作品为可运行的程序（如网页应用、智能体应用），需提供可运行的链接或应用程序源代码。对于网页应用，确保链接稳定可访问。」

**本项目应对策略：三重冗余**（任一失效仍有兜底）：

| 层级 | 形态 | 作用 | 地址 | 状态 |
|---|---|---|---|---|
| **L1** | 线上真实系统（完整功能，**CloudBase 云开发 · 免费体验环境**，境内部署） | 证明系统真实运行 | `https://qwer-d4gf2r76o8829463b.service.tcloudbase.com/` | ✅ 实测 HTTP 200（2026-09-19 部署）：`/api/health` 只读 / `/api/graph` **7 主题·32 来源·148 证据** / `POST /api/ask` **实时命中 6 条**（中国经济网·央视网） |
| **L2** | **免登录只读演示入口** | 降低评委访问门槛（无需注册） | 同上（云端即只读：写方法一律 403） | ✅ 实测 200（`readonly: true`、`sessions_tracked: 0`） |
| **L2b** | 备选长期入口（PythonAnywhere 免费档，境外） | 双平台冗余 | `https://lxh456.pythonanywhere.com/` | ✅ 实测 200；⚠️ **该平台免费档出站受白名单限制 → 实时核证在该入口不可用**，仅作界面与离线语料兜底（已如实标注） |
| **L3** | **静态快照站 · 公网长期地址（GitHub Pages）** | 线上系统不可达或网络异常时仍可查看界面、问答流程与证据溯源；**长期有效（无到期日）** | `https://lxh113377.github.io/zhengwen/` | ✅ **2026-09-19 实测 HTTP 200**（首页 34,357 B；`assets/` 4 图全 200） |
| **L3b** | **前端展示 · 3D 叙事页（GitHub Pages）** | 展示滚轮驱动 3D 叙事与界面形态 | `https://lxh113377.github.io/zhengwen/demo/` | ✅ 实测 200（页面 + css/js/vendor 全部 200；**修复过程见变更记录**）；⚠️ **静态托管、后端未连接 → 问答不可用**，页面已显著标注并指向 L3 与本地运行方式 |
| **L4** | **源码仓库（GitHub 公开）** | 满足官方「可运行链接**或**源代码」中的源码路径，评委可自助复现 | `https://github.com/lxh113377/zhengwen` | ✅ 已公开（132 文件入库）；**公开前对每个待入库文件逐一扫描密钥，命中 0** |
| **L3-离线** | 静态快照兜底站（离线文件形态） | 网络完全不可达时可直接打开 | `交付物/提交包/静态快照站/index.html`（单文件 + 4 图，**零外部依赖，可离线打开**） | ✅ index 与全部图片 HTTP 200，**断链 0** |

> 🔴 **入口纪律（2026-09-19 用户永久规则）**：**禁止使用任何临时部署地址**（云沙箱 / 短时效预览链接等）作为对外入口或交付链接。此前使用的 `*.codebuddy.cloudstudio.run` 沙箱已**全部停用**并从材料中移除（**仅变更记录保留历史留痕，按 R241 不改写**）。对外入口一律走**长期通道**：CloudBase 体验环境（到期 **2027-03-14**，覆盖 9/30 提交—10 月复赛—11 月总决赛）、PythonAnywhere 免费档（L2b）、**GitHub Pages（L3 / L3b，无到期日）**、**GitHub 公开源码仓库（L4）**。

> **为什么必须有 L2**：复赛为**线上评选**（29 号第 5 页），评委按链接访问。要求评委注册账号 = 主动提高访问门槛 = 主动丢「用户体验 10 分」。

**本地运行（等价于线上，零第三方依赖）**：

```text
python app.py                       # 完整演示   → http://127.0.0.1:8848/
python app.py --readonly            # 只读演示   → http://127.0.0.1:8848/demo
python app.py --readonly --host 0.0.0.0 --port 8848   # 云平台部署用法
```

---

## 二、L2 免登录只读入口：设计要求与实测结果

| # | 要求 | 实测结果 | 证据 |
|---|---|---|---|
| 1 | **零注册、零登录**，打开即用 | ✅ 访问即用，无任何登录态 | 云沙箱实测 `/demo` 200 |
| 2 | **只读**：不写库、不改数据 | ✅ 服务端强制：`PUT` / `DELETE` / `PATCH` 与「非 `/api/ask` 的 `POST`」一律返回 **403**；只读模式下**不写入会话状态** | 实测：三次问答前后 `/api/health.sessions_tracked` **恒为 0** |
| 3 | 页面显著标注「演示环境 · 只读」 | ✅ 顶部粉色横幅：「演示环境 · 只读 ｜ 免登录访问、不写入任何数据……」 | 页面元素 `#readonly-banner` |
| 4 | 演示数据带徽章 | ✅ 顶栏徽章显示 **「合成演示 + 公开来源」**（鼠标悬停显示逐源许可） | `/api/health.badge` 与 `sources[]` |
| 5 | 模型徽章如实反映实际生成路径 | ✅ **线上已注入凭据**：应放行问题走 `mode=model` / `provider=DeepSeek`（线上实测 **2476—3653 ms**，含网络往返；4 条证据引用）；应拒答问题仍由闸门拦截走 `mode=rule`，**规则版结果绝不显示「AI 生成」徽章**。凭据仅经运行环境注入、仓库零落盘；移除凭据自动回到「规则版 + 原因」 | 问答响应 `mode` / `provider` / `degraded_reason` |
| 6 | 移动端可访问（响应式） | ✅ 375×812 实测无横向溢出（含只读横幅） | AC-OBS-09 实测记录 |

**评委可自行验证的只读命令**（对任一入口执行）：

```text
GET  /api/health            → 200，readonly: true
GET  /demo                  → 200
POST /api/ask {"query":"…"} → 200（只读问答：仅读语料，不产生副作用）
PUT  /anything              → 403（只读环境拒绝写操作）
GET  /api/health            → sessions_tracked 始终为 0（证明无写入副作用）
```

---

## 三、源码复现说明（评委可自行验证）

```text
1. 解压源码包（或进入工程佐证材料/05-源代码全文/）
2. 进入 src/ 目录                    # 无需安装任何第三方依赖
3. 启动服务： python app.py           # Windows 亦可双击 run.cmd
   只读模式： python app.py --readonly  （或双击 run-readonly.cmd）
4. 浏览器访问： http://127.0.0.1:8848/     （只读入口 /demo）
5. 运行回归自检（三项，均须 exit=0）：
     python rag.py                          # 离线检索与拒答   期望 23/23
     python live.py --selftest              # 实时核证分层判据  期望 41/41
     python tools/verify_layered_output.py  # 分层输出端到端   期望 19/19（需先启动 app.py）
6. 重建开放数据语料： python tools/fetch_worldbank.py --from-raw   # 离线；去掉 --from-raw 则联网重抓
7. 生成静态快照站： python tools/build_snapshot.py
8. 生成《应用方案》PDF： python report/render_pdf.py   # 自动校验页数 ≤20
9. 查看实时源登记表： GET /api/live/sources            # 12 个可用源 / 19 项实测不可用（含失败形态）
10. 导出评审用证据快照： python tools/export_live_evidence.py     # 9 用例完整响应（需先启动 app.py）
```

| 项 | 内容 |
|---|---|
| 源码位置 | `工程佐证材料/05-源代码全文/src/`（**56 文件 / 1,873,363 字节**；与开发工作区 `src/` 一致，已排除 `__pycache__` 与 `.pyc`）。⚠️ 该数字随修订变动，**以 `sync_source_copy.py` 双向比对（MISSING / DIFF / EXTRA 三类须全为 0）与哈希台账为准**；2026-09-19 首次填充时该目录为空（当日 50 文件）→ 实时核证层与工具上线增至 55 → **第三轮扩源与分层输出增至 56** |
| 实时源登记表 | `实时源登记表.md`（与 `GET /api/live/sources` **同源**，由 `src/tools/export_live_evidence.py` 导出）：**12 个可用源 + 19 项实测不可用（含失败形态）**。⚠️ 该文件此前仅被材料**引用**而磁盘并不存在（隐性断链），第三轮已补齐 |
| 运行环境 | **Python 3.10+**（开发与实测环境：Python 3.12.2 / Windows）；前端为原生 HTML/CSS/JS，无需 Node |
| 第三方依赖 | **运行时零依赖**（仅标准库）；`report/render_pdf.py` 需要 Playwright（仅用于生成 PDF，不影响系统运行） |
| 环境变量 | `HOST`（默认 127.0.0.1）、`PORT`（默认 8848）、`READONLY`（1 = 只读模式）、`LLM_API_KEY`（可选，配置后启用在线模型路径）；**包内不含任何密钥** |
| 冷启动验证 | ⬜ 待做（在干净目录解压后独立跑通）—— 列为提交前验收项 |

### 数据可复现性（重点：让「数据从哪来」可被独立复核）

```text
src/tools/fetch_worldbank.py      # 抓取世界银行 WDI 指标（CC BY 4.0）
  └─ 原始 API 响应整包落盘 → src/corpus/_raw/*.json     # 审计留痕，可逐字节比对
  └─ 派生语料           → src/corpus/wb_real.json        # 删掉可一条命令重建
  └─ --from-raw 模式：不联网，用已落盘快照重建（离线复现）
```

---

## 四、第三方组件与许可（29 号第 4 页要求核实知识产权）

| 组件 | 用途 | 许可证 | 状态 |
|---|---|---|---|
| Three.js | WebGL 3D 渲染（程序化几何） | **MIT** | ✅ 已确认 |
| GSAP / ScrollTrigger | 滚动驱动动画 | **GSAP 标准 "No Charge" 许可**（2025-04-30 生效，版权方 Webflow）：**含商业用途可免费使用**；唯一需事先书面同意（非付费）的情形是把它用于「与 Webflow 可视化动画构建产品直接竞争的可视化拖拽式动画构建工具」——本作品是对话式问答产品，不构成竞争 | ✅ **已核实，合规** |
| Playwright（仅构建期） | HTML → PDF 排版 | Apache-2.0 | ✅ 仅用于生成方案 PDF，不进入运行时 |
| Python 标准库 | 服务端 / 检索内核 | PSF License | ✅ |
| 字体 | 页面与 PDF 使用系统字体（PingFang SC / Microsoft YaHei / SimSun 等），**不使用商业字体文件** | 随系统 | ✅ |
| 语料 | S0 自建合成（原创）+ 世界银行开放数据（**CC BY 4.0**） | 见《语料来源台账》 | ✅ 逐源登记 |

> 🔴 **纪律**：GSAP 许可条款已在《应用方案》「开发工具」一节如实说明；许可原文与访问日期见 `语料来源台账.md` §七。

---

## 五、免责与边界声明（随材料一并提交）

> 本作品展示环境使用**合成演示数据**（界面徽章标注「合成演示」）与**真实开放数据**（世界银行，CC BY 4.0，界面与证据逐条标注许可与原文入口）；
> 引用的公开资料见《应用方案》附录 A 来源台账（含 URL 与访问日期）；
> AI 输出为**参考信息**，不替代用户判断；系统在无依据时**明确拒答**，在模型不可用时自动降级为规则版并如实标注来源与原因。
> 实时核证采用**两层输出**：**可核实依据**（原始词命中 ≥2）才进结论并参与拒答判定；
> **相关线索**（命中 ≥1）仅展示并标注「未核实」，不进结论、不进模型输入。
> 实时源覆盖**有限且已如实披露**（12 可用 / 19 实测不可用，逐条留痕）；**不得理解为「覆盖全网」**。
>
> 2026-09-19 扩源：新增 **360 资讯·聚合检索通道**（服务端渲染、原文直链、**按 22 项权威域白名单过滤**），
> 使「任意问题」的检索面从各媒体首页池扩到全网；同批实测**百度资讯聚合不启用**（同 IP 第 3 次请求起持续被反爬拦截、
> 且未取得链接结构样本 → 按「宁少不假」留档不启用）、**联合国中文 RSS 实测 404**（原为可用源，按纪律暂置不可用）。
> 拒答判据**未放松**（原始词命中 ≥2 才算可核实依据）。
> 世界银行未参与、未背书本项目；本作品不使用其名称与标识。

---

## 六、变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-18 | 建立骨架（三层入口设计 + 复现步骤占位 + 第三方许可待核实） |
| 2026-09-18（第二轮） | **填真实地址与实测结果**：云沙箱只读入口已部署、静态快照站已产出；只读语义 6 项要求逐条实测；复现步骤改为真实命令（8 步）；**GSAP 许可核实完成**（标准免费许可含商用）；Playwright 登记为构建期组件 |
| 2026-09-19 | 🔴 **重部署并更换入口地址**：原沙箱地址 `b70f222a….codebuddy.cloudstudio.run` 实测已失效（**HTTP 400**），若未发现将导致评委按链接访问失败 → 重新部署，新地址 `9e82bf3c….codebuddy.cloudstudio.run`，实测 `/demo` 200 / `PUT` 403 / 148 证据。**凭据已注入线上**（经启动命令环境变量，不写入仓库文件）：`/api/health` 返回 `model_available: true`、`provider: DeepSeek`；线上实测应放行问题走 `mode=model`（1175—1437 ms），应拒答问题仍由闸门拦截走 `mode=rule` |
| 2026-09-19（第二轮入口更换） | **换新为由本机管理的实例**：`9e82bf3c…` 虽仍实测可达，但它**不在本机部署清单内**（本机仅 `0e4efc26` / `47c0c9de` 两条记录，且实测 **400 已失效**）→ 无法由本机重建/维护。重新部署为 `adf6ce20….codebuddy.cloudstudio.run`（本机管理，可随时重建），实测 `/` 200、`/demo` 200、`PUT` **403**、`/api/health` 200（148 证据、`readonly: true`、`sessions_tracked: 0`）、`POST /api/ask` 200（`mode=rule`，4 条引用）。⚠️ **新实例未注入模型凭据**（本机不持有密钥，且密钥禁止落盘）→ 在线模型路径暂走规则版并如实标注；凭据到位后注入即可恢复 `mode=model`。三处引用旧地址的文档已同步替换 |
| 2026-09-19（第三轮） | **凭据注入新实例并复测（R242 状态回扫）**：凭据经运行环境注入，**不落盘任何文件、不进仓库**（门禁密钥扫描 0 命中）。`/api/health` → `model_available: true` / `provider: DeepSeek`；线上实测：应放行问题 `mode=model`（**2476—3653 ms**，含网络往返，4 条证据引用），应拒答问题仍由闸门拦截走 `mode=rule`（「今天天气怎么样？」→ 拒答，原因「5 个检索词均不在语料词表内」）。三处文档中「未注入凭据 / 走规则版」的表述已同步改回 |
| 2026-09-19（第十轮） | **新增 GitHub Pages 长期入口（L3 / L3b / L4）**：建公开仓库 `lxh113377/zhengwen`，以 `docs/` 作 Pages 源（根 = 静态快照站，`/demo/` + `/web/` = 前端展示副本），同时公开源码与参赛材料（132 文件）。**实测发现并修复 1 个真实缺陷**：`src/web/index.html` 使用**域名根绝对路径** `/web/...`，在 project pages（站点根为 `/zhengwen/`）下被解析为 `https://lxh113377.github.io/web/...` → **css / js / vendor 三项实测 404**（页面会白屏）；改为相对路径 `../web/...` 后全部 **200**（**仅改 Pages 派生副本，`src/web` 权威源保持不动**）。**公开前纪律**：对全部 132 个待入库文件**逐一**扫描密钥形态，**命中 0**；`src/run.cmd` 与 `run-readonly.cmd` 此前被忽略规则排除，而本文正文引用了它们（隐性断链，R240）→ 已补入仓库 |
