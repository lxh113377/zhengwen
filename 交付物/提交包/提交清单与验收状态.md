# 提交清单与验收状态

> 作品：**待定**（方案探索中，见 `交付物/_参考资料-非提交/2026-09-18-参赛材料评审与优化分析报告.md` §5）
> 赛事：2026 年 iCAN 大学生创新创业大赛 · AI 应用创新挑战赛 · 软件赛道 · 高校组
> 提交截止：**2026-09-30**
> 清单建立：2026-09-18 ｜ 状态：**骨架已建，材料待产出**

本目录 `交付物/提交包/` 是**最终上传目录**。工作空间（`src/`、`memory/`）与交付物严格分离。

---

## 一、官方三项硬约束

| # | 官方要求（29 号第 5—6 页原文） | 硬约束 | 本包位置 | 状态 |
|---|---|---|---|---|
| 1 | 应用方案 | PDF，**≤ 20 页** | `证闻-应用方案-2026-09-30.pdf` | ✅ **已产出：19 页**（自动页数校验通过；渲染脚本 `src/report/render_pdf.py`）。**2026-09-19 第七轮重渲染**：修正两张截图规格错配（命名 1440 实为 375x812 → 1440x900），页数由 19 降至 17；**第八轮**：实时核证层上线补入第 7.5 / 10.1a 节，并允许长表跨页消除空白 → **18 页**；**第九轮（同日）**：实时召回扩展 + 分层输出，重写 §7.5 / §10.1a / 评审问答并补入两条边界披露 → **19 页**（留 1 页余量） |
| 2 | 演示视频 | MP4，**≤ 5 分钟**，须突出智能体运行过程（接收请求 → 分析决策 → 反馈结果）与模块协同 | `工程佐证材料/08-视频候选/` | 🔴 未产出 ｜ **归属：非本机成员负责**（2026-09-19 用户确认），本机暂不产出 |
| 3 | 可运行程序 | 可运行链接**或**源代码；网页应用**须确保链接稳定可访问** | `00-可运行链接与源码说明.md` + `静态快照站/` | ✅ **三层已就位**（L1 本地服务 / L2 云沙箱只读入口 / L3 静态快照兜底，均实测 200 且免登录） |

---

## 二、根级交付件

| 文件 | 说明 | 状态 |
|---|---|---|
| `参赛口径-单一真相源.md` | 术语与红线唯一来源 | ✅ 已建立 |
| `材料数字对照表.md` | 数字唯一来源 | ✅ 已建立（**已登记 PDF 页数、语料规模、回归条数等实测值**） |
| `提交清单与验收状态.md` | 本文件 | ✅ 已建立 |
| `00-可运行链接与源码说明.md` | 线上入口、只读演示、源码复现方式 | ✅ **已填真实地址与实测结果** |
| `证闻-应用方案-2026-09-30.pdf` | 应用方案终版（18 页） | ✅ **已产出** |
| `语料来源台账.md` | 语料与第三方组件许可唯一来源 | ✅ 已建立（S0 + 世界银行 CC BY 4.0 已接入） |
| `静态快照站/` | 单文件 HTML 兜底站（零外部依赖，可离线打开） | ✅ **已产出**（index + 4 图全 200） |

---

## 三、《应用方案》官方 10 项覆盖检查（逐项打勾）

> 依据 29 号第 5—6 页原文列举的内容要求。**缺任一项 = 展示效果 10 分直接失分。**

| # | 官方要求内容 | 方案章节 | 状态 |
|---|---|---|---|
| 1 | 项目背景 | 第 1 章 | ✅ |
| 2 | 当前存在的痛点问题 | 第 2 章（含 4 条 [公开来源] 硬数据 + 限定说明） | ✅ |
| 3 | 需求分析 | 第 3 章（含非功能需求） | ✅ |
| 4 | 目标用户群体及功能需求 | 第 4 章（4 类用户 + F1—F9 功能清单） | ✅ |
| 5 | 开发工具 | 第 5 章 §5.1（含逐组件许可） | ✅ |
| 6 | 技术方案 | 第 5 章 §5.2（模块职责 + 关键文件） | ✅ |
| 7 | **逐一说明作品功能，重点突出 AI 功能在作品中的核心作用** | 第 6 章（AI 五层核心作用 + F1—F9 逐项 + 关键工程决策） | ✅ |
| 8 | **完整使用说明（用户操作流程、交互指南）** | 第 8 章（访问方式 / 交互 / 提问流程 / 推荐演示问题 / FAQ / 本地复现） | ✅ |
| 9 | 应用前景 | 第 9 章 §9.1 + §9.3 | ✅ |
| 10 | 商业模式 | 第 9 章 §9.2（三档，均标注 [试点假设]，明确未执行） | ✅ |

---

## 四、提交前验收清单（全部通过才可提交）

| # | 验收项 | 判据 | 状态 |
|---|---|---|---|
| 1 | PDF 页数 | ≤ 20 页 | ✅ **18 页**（`render_pdf.py` 自动校验，超限即报错不产出；留 2 页余量） |
| 2 | 视频时长与格式 | ≤ 300 秒 / MP4 | 🔴 未产出 |
| 3 | 链接可达性 | 线上系统 / 免登录只读 / 静态快照 均 HTTP 200 | ✅ 三层实测 200（云沙箱只读入口 + 快照站 index/4 图） |
| 4 | 盲审扫描 | 学校名 / 校徽 / 教师姓名 / 单位名 命中 **0** | ✅ 方案 HTML 与 PDF 均 0 命中 |
| 5 | 口径扫描 | 旧赛事名 / 旧组别 /「省一等奖」/「低代码」命中 **0** | ✅ 方案 HTML 扫描 0 命中 |
| 6 | 数字一致性 | 方案 ↔ 视频 ↔ 链接说明 ↔ 数字对照表 无冲突 | 🟡 **方案 ↔ 链接说明 ↔ 数字对照表 ↔ 答辩稿 已实测一致（2026-09-19 全文数字抽取比对，冲突 0 处）**；**视频未产出**，产出后须补验 |
| 7 | 数字标签 | 全部数字带 `[合成演示]`/`[公开来源]`/`[试点假设]` 之一 | ✅ 方案内数字均已标注（痛点数据 [公开来源]、实测值注明可复跑、商业预期 [试点假设]） |
| 8 | 数据真实性 | 无"已访谈真实用户"类表述（除非附调研方法说明） | ✅ 方案明确声明未与机构合作、无用户调研；公开来源附 URL + 访问日期 + 样本限定 |
| 9 | 界面错误态 | 无裸错误串 / 堆栈 / 内部路径 / 本机绝对路径 | ✅ 快照站与 PDF 扫描 **0** 命中（含 `C:\Users` 与 `sk-` 密钥形态） |
| 10 | 降级徽章 | 规则版结果不显示"AI 生成"；不可用时显示「规则版 + 原因」 | ✅ 未配置凭据时徽章为「规则版」，并显示降级原因 |
| 11 | 团队一致性 | 与官网报名信息逐字一致；指导教师未列为成员 | ⬜ **待团队名单（建议 9/27 前）** |
| 12 | 提交包哈希 | SHA256 台账落盘 | ✅ **已复跑（2026-09-19 第九轮，末次）**：`提交包-SHA256台账.md` + `工程佐证材料/submit_manifest.json`；工具 `src/tools/hash_manifest.py` **幂等已验证**（连跑两次逐文件哈希完全一致）。⚠️ **文件数与总字节数以台账自身为准**（本目录每修订一次即变动，写死必然二次失准；历次实测 31 → 85 → 97 → 104 → **105** 文件，均为各轮当日快照）；**提交前须末次复跑** |
| 13 | 首屏加载实测 | 各场景 FCP 已实测并登记，满足首屏 3 秒设计目标 | ✅ **已通过**（FCP 200—296 ms；二次访问 20 ms；证据 `AC-OBS-21-首屏加载与滚动帧率实测记录.md`） |
| 14 | 帧率口径纪律 | 材料不出现「稳定 60 fps」等未取得的绝对显示帧率 | ✅ **已通过**（本机 rAF 不受 vsync 约束，绝对帧率不可得；材料只写相对结论：滚动全程 <30 fps 帧占比 0.0%） |
| 15 | 外部资源阻塞风险 | 第三方资源不可达（含挂起）时页面仍可渲染 | ✅ **已通过**（字体请求永不响应时 FCP 272/288 ms；修复前为 40 s 零绘制 —— 已修复） |

---

## 五、当前可复核入口

| 入口 | 地址 | 状态 |
|---|---|---|
| 线上真实系统（免登录只读，**CloudBase 云开发 · 免费体验环境**） | `https://qwer-d4gf2r76o8829463b.service.tcloudbase.com/` | ✅ 实测 200（只读模式；`/api/health` 显示 `readonly=true`、`sessions_tracked=0`、**12 可用源 / 19 项实测不可用**；`/api/graph` **7 主题·32 来源·148 证据**；`POST /api/ask`「加装电梯业主需要出多少钱？」**实时可核实 6 条**，越界问题仍拒答）。⚠️ 该实例**未注入模型凭据 → 走规则版并如实标注**；体验环境到期 **2027-03-14**（覆盖全赛程）。（2026-09-19） |
| 免登录只读演示（同实例根路径） | 同上地址 `/` | ✅ 零注册、零写入（`PUT` → **403**；问答前后 `sessions_tracked` 恒为 0）。**凭据已注入** → 应放行问题走 `mode=model`，应拒答问题仍由闸门拦截走 `mode=rule`（规则版不显示「AI 生成」） |
| 静态快照兜底 | `交付物/提交包/静态快照站/index.html`（单文件 + 4 图，离线可开） | ✅ index 与全部图片 HTTP 200，断链 0 |

> 只读语义可自行验证：对线上地址发 `PUT` / `DELETE` / 非 `/api/ask` 的 `POST` → 一律 **403**；`POST /api/ask` 正常返回问答结果。

---

## 六、变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-18 | 建立本清单；三份口径文件就位；三项硬约束登记完成；官方 10 项覆盖检查表建立 |
| 2026-09-18（第二轮） | **《应用方案》PDF 产出（19 页）**；官方 10 项全部对应到章节；**三层可运行入口全部就位并实测 200**（云沙箱只读入口 + 静态快照站）；验收清单 1/3/4/5/7/8/9/10 已通过；新增语料来源台账与快照站条目 |
| 2026-09-19 | **提交包 SHA256 台账产出**（验收项 12 转 ✅，31 文件 / 2,309,753 字节，幂等已验证）；**CNIPA 商标检索留痕**入 `03-工程验证证据/`（三条官方路径实测全部不可得，对照组可证 0 行属后端故障 → 材料维持「未检索到冲突证据」）；演示视频标注**归属非本机成员**；三项门禁复测（口径 / 盲审 / 洁净）均 **0 命中** |
| 2026-09-18（第三轮） | **首屏加载与滚动帧率实测闭环**：新建可复跑工具 `src/tools/measure_perf.py`（10 场景加载矩阵 + 双模式帧率 + 降级双层验证）；实测中发现并修复 **2 个缺陷**（#10 Google Fonts render-blocking 导致字体挂起时 40 s 零绘制 → 改为非阻塞，FCP 1132→256 ms；#11 低帧率自适应判据按帧计数在极低帧率下失效 → 改为挂钟时间判据，×100 限速下由不触发变为触发）；新增验收项 13/14/15；数字已登记进 `材料数字对照表.md` 二·8—15；新增 `AC-OBS-21-首屏加载与滚动帧率实测记录.md` + 三份原始实测 JSON |
| 2026-09-19（第二轮） | **提交包完整性补齐 + 数字一致性实测**：① 填充此前为空的 `04-部署方案/`、`05-源代码全文/`（**50 文件 / 1,491,596 字节** 源码副本）、`07-演示数据导出/`（2 份语料，SHA256 与 `src/corpus/` 逐字节同源）；删除与 `06-运行实拍截图/` 重复的空目录 `01-演示快照集/` ② **修复回归用例数漂移** 16/10/6 → **23/15/8**（`src/README.md`、`memory/` 2 处改当前值；**提交包内历史日志保留当日原值 + 增校正注**）③ **方案全文数字抽取比对，冲突 0 处**，`材料数字对照表.md` §六 五项验收实测勾选 ④ 台账复跑（**85 文件 / 3,876,963 字节**）⑤ 门禁复测（口径 / 盲审 / 洁净）均 **0 命中** ⑥ 产出 `_参考资料-非提交/2026-09-19-答辩证据地图.md`（内部备赛，**不进上传包**） |
| 2026-09-19（第三轮） | **在线模型路径跑通（P0 闭环）**：凭据经环境变量注入（🔴 **不落盘任何文件**，门禁密钥扫描 0 命中、PDF 抽文本 0 命中）；**5/5 `mode=model`**（DeepSeek / `deepseek-chat`），耗时 **1351—2299 ms**，**引用越界丢弃 0/5**；应拒答 **2/2 未调用模型**即被闸门拦截。新增 `AC-OBS-23-在线模型路径实测记录.md` + `raw/model-20260919-012728.json`（60,394 字节，已自检不含密钥）；《材料数字对照表》三·1 / 三·2b / 三·2c / 三·2d / 三·7 登记实测值；**方案 PDF 同步重渲染，仍 19 页 ≤20**，消除「尚未接入凭据」过时表述（PDF 抽文本核验：新表述命中 1、旧表述 0）。⚠️ 样本仅 5 例，材料中不外推成功率 |
| 2026-09-19（第四轮） | 🔴 **线上入口失效并重部署 + 凭据注入线上**：原地址 `b70f222a…` 实测 **HTTP 400 已失效**（历史部署记录不可信）→ 重新部署为 `9e82bf3c…`，实测 `/demo` 200、`PUT` 403、`/api/health` 200（148 证据、`readonly: true`）。**在线模型凭据已注入线上**（启动命令环境变量，仓库零落盘）：`/api/health` → `model_available: true` / `provider: DeepSeek`；线上实测应放行 `mode=model`（**1175—1437 ms**）、应拒答仍 `mode=rule`。三处引用旧地址的文档（本文件 / `00-可运行链接与源码说明.md` / `04-部署方案/部署方案.md`）已同步替换。**此例即为 R242「状态变化后须回扫陈述」的实证** |
| 2026-09-19（第五轮） | **换用本机可管理的实例**：`9e82bf3c…` 实测仍 200，但**不在本机部署清单内**（本机仅 `0e4efc26` / `47c0c9de` 两条记录，实测 **400 已失效**）→ 无法重建维护。重新部署 `adf6ce20…`（本机管理），上架自检 7 项实测：`/` 200、`/demo` 200、`/api/health` 200（`readonly=true`、148 证据、`sessions_tracked=0`）、`POST /api/ask` 200、`PUT` **403**、`model_available=false`（**未注入凭据** → 走规则版，如实标注）、`python rag.py` 23/23。三处文档地址已同步。⚠️ **待办：模型凭据未注入**（密钥禁落盘，本机当前不持有）→ 凭据到位后经启动命令环境变量注入并复测，届时回扫本文件与 `00-` 文档中的模型状态表述 |
| 2026-09-19（第六轮） | ✅ **待办闭环：凭据已注入新实例**：`/api/health` → `model_available: true` / `provider: DeepSeek`；线上实测应放行 `mode=model`（**2476—3653 ms**，含网络往返）、应拒答仍 `mode=rule`；`PUT` 403 与 `sessions_tracked=0` 未变。凭据**仅经运行环境变量传递，零落盘**（提交包密钥扫描 0 命中）。三处文档中「未注入凭据」表述已按 **R242** 全部回扫改回。⏳ 仍待办：演示视频 MP4、团队名单 |
| 2026-09-19（第八轮） | **实时核证层上线 + 3D 滚动联动缺陷修复**（用户两项指令）：① 3D 后半段不随滚动条移动 —— 根因实测为「页面高度由接口返回后注入的内容决定，而滚动库不感知 DOM 内容注入」，字体域名挂起 / 接口晚于 load 时其缓存 `end` 停在 3600（正确值 5714）→ 滚动条过 63% 后进度恒 1；改为实时读元素几何 + 注入后显式刷新，新增回归套件 **3/3 通过**。② 实时查证 —— 新增 `src/live.py` 双轨并行（离线语料 + 提问时并发的权威源），**可用 7 源 / 实测不可用 10 项逐条留痕**；**两条路都没可核实依据才拒答**；3D 页切换为「本次检索到的证据网络」（无实时证据时回落离线图谱并标注原因）。验证：`rag.py` 23/23、`live.py --selftest` 23/23（含真实源对照）、滚动联动 3/3、浏览器实测 JS 错误 0。交付包：源码副本三类差异 **0**、台账 98 文件、四项门禁 **0 命中**、方案 PDF 重渲染 **18 页**。另将「取服务行为证据前须确认服务晚于源码修改」升为全局规则 **R252（V10.47.0）**，落点工具 `src/tools/check_service_freshness.py` |
| 2026-09-19（第七轮） | **文本框透明度统一（用户报障修复）**：`src/web/css/main.css` 由 6 种散落取值（.58/.60/.86/.90/.93/.985）收敛为**表面三档变量**（L1 面板 .60 / L2 面板内卡片 .70 / L3 控件与浮层 .82；悬停 .86、聚焦 .94 三态保留）；对话坞 .93→.82、输入框实色→.82、composer 底栏去掉叠层。**浏览器计算样式实测 11/11 一致**（含 hover/focus 对照组）。连带修正：`viewport-1440-s1.png` 与 `reduced-motion-1440.png` **命名标称 1440、实为 375x812**（规格错配）→ `06-运行实拍截图/` 8 张按标称规格重拍；**方案 PDF 重渲染 19 → 17 页**（≤20，自动校验通过）；静态快照站重建；`05-源代码全文/` 副本同步（SHA256 一致）；哈希台账复跑 |
| 2026-09-19（第九轮） | **实时召回扩展 + 分层输出（用户报障「拒答率太高、像固定语料的假查证」）**。**先诊断后动手**：改造前实测「英伟达最新财报」「2026 年诺贝尔文学奖得主」实时 **7 源全 0 命中** → 根因是**召回不足而非闸门过严**，故**不放松闸门**。① **来源实测**：新增可用源 7 → **12**（**央视网站内检索**为真关键词检索、支持翻页；中国新闻网滚动页 145 条/页、中国日报 136、中国青年网 129、中国经济网 142）；实测不可用 10 → **16** 项逐条留痕；**推翻旧结论**「央视网站内检索不可用」（原判定只看表单壳，未识别服务端渲染结果 → 原登记保留并注明「曾误判、后更正」，不删改历史）。② **分层输出**：新增 `related`（相关线索）层 —— 判据 ≥1、最多 5 条、**不参与拒答判定**、不进结论、不进模型输入，界面独立成块标注「未核实」。③ **顺带修 2 个真实缺陷**：列表页普通相对路径条目被静默丢弃（`_abs_url` 未解析相对路径）；央视网条目「日期未标注」（新增 URL 日期兜底 + 日期归一）。④ **时效不伪装**：含时效词且命中均为旧闻时显式提示最新发布日。**验证**：`rag.py` **23/23**、`live.py --selftest` **41/41**、`verify_layered_output.py` **19/19**（正样本 3 问增益 + 反样本 2 问仍拒答 + 分层不越界）。**两处行为边界变更已如实披露**（天气类问题现能取回真实天气新闻；生活服务类问题在来源确有条目时会取回，说明本层不区分新闻/非新闻意图）。**交付同步**：源码副本三类差异 **8 DIFF + 1 MISSING → 0/0/0**；台账 **104 文件 / 5,473,069 字节**；方案 PDF 重渲染 **19 页**；新增证据记录 `AC-OBS-28` 与 3 份原始输出。⚠️ **凭据核查**：用户提供的 `sk-` 凭据实测为 **DeepSeek 模型 key**（博查 401 / Tavily 401 / Serper 403），**通用检索插槽仍为未配置**，材料按实标注 |
